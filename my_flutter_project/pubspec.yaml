name: my_flutter_project
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter

  # Add the flutter_signin_button package here
  flutter_signin_button: ^2.0.0

  # Add shared_preferences for local storage
  shared_preferences: ^2.0.15

  # Use file_picker for selecting profile pictures on web
  file_picker: ^5.0.1  # Use file_picker instead of image_picker for web

  # The following adds the Cupertino Icons font to your application.
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/google_icon.png
    - assets/images/facebook_icon.png
